server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/flowrspot?useSSL=false
    username: root
    password: root
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect
        show_sql: true
        format_sql: true

jwt:
  secret: PurkmRDAnAaSvmDaAfi

quote:
  api:
    link: http://quotes.rest/qod